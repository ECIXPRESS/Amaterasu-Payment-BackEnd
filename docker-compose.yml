version: "3.9"

services:
  payment-service:
    build: .
    container_name: payment-service
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    networks:
      - eciexpress-network

networks:
  eciexpress-network:
    driver: bridge